services:
  db:
    build:
      context: .
      dockerfile: dockerfiles/db.Dockerfile
    ports:
      - '5433:5432'
    restart: always
    env_file:
      - .env
    volumes:
      - db_volume:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD", "pg_isready", "-h", "localhost", "-p", "5432", "-q", "-U", "postgres"]
      interval: 30s
      timeout: 30s
      retries: 8
  server:
    build:
      context: .
      dockerfile: dockerfiles/server.Dockerfile
    ports:
      - '8080:8080'
    environment:
      # La app fija su zona horaria por configuracion (ZonaHorariaConfig); esto
      # alinea ademas el reloj del sistema del contenedor, que por defecto
      # arranca en UTC, para que los logs no digan otra hora que la aplicacion.
      TZ: America/Argentina/Buenos_Aires
    env_file:
      - .env
    depends_on:
      db:
        condition: service_healthy

volumes:
  db_volume:
